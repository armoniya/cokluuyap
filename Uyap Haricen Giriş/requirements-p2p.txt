# P2P (WebRTC) uzaktan erişim bileşenleri için ek bağımlılıklar.
# uyap_proxy/uyap_giris zaten httpx + fastapi + uvicorn kullanıyor; bunlar ofis ajanında
# da gerekli olduğundan onları da ekledim.

# Ofis ajanı (office_agent.py):
aiortc>=1.6
websockets>=12
httpx
fastapi
uvicorn
requests

# Ev istemcisi (home_client.py):
#   aiortc>=1.6
#   websockets>=12
aiohttp>=3.9

# Signaling sunucusu (signaling_server.py) — sadece:
#   websockets>=12
